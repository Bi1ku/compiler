#include "../include/lexer.h"
#include <stdio.h>
#include <stdlib.h>

// TODO: Determine "PEMDAS" for code to get action tree
char parser(token tokens[]) {
  printf("PARSER");
  return EXIT_SUCCESS;
}
